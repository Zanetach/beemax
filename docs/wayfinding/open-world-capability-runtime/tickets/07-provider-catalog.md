# Generalize the trusted Provider catalog and acquisition pipeline

Label: `wayfinder:task`

## Question

What signed Provider manifest, installer interface, dependency lock, sandbox, configuration reference, health probe, capability receipt, quarantine, rollback, and allowlist policy can generalize today's single pinned provider adapter without permitting model-authored arbitrary installation commands?
