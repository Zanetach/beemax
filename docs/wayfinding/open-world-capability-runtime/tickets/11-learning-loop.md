# Build the evidence-gated learning and curation loop

Label: `wayfinder:task`

## Question

How should successful and failed executions propose Skill patches, new Skills, provider preferences, verifier improvements, or declarative workflows; run representative independent trials; prevent task-specific overfitting and prompt injection; promote immutable versions; measure downstream value; and archive or roll back regressions?
