# Add progressive Tool search, describe, activate, and call

Label: `wayfinder:task`

## Question

How should BeeMax expose a small always-available control waist plus on-demand `capability_search`, `capability_describe`, activation, and invocation so thousands of Tool/MCP schemas remain discoverable without consuming every model turn, while preserving the exact Tool Runtime policy, Governance, Effect, trace, and receipt path of direct calls?
