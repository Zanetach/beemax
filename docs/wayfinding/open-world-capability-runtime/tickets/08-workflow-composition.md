# Add sandboxed declarative workflow composition

Label: `wayfinder:task`

## Question

How can BeeMax synthesize a bounded workflow from existing typed Tools when no single Tool completes an outcome, execute it in an isolated runtime, pass only declared data between steps, reuse the same governance path for every underlying call, and promote it only after independent real trials prove it reusable?
